apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: readsbpb-rrd
  namespace: adsb
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
  storageClassName: longhorn   # switch from local-path to longhorn

