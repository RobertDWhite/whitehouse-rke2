apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: readsbpb-rrd
  namespace: adsb
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 500Mi
