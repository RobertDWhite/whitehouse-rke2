apiVersion: v1
kind: Service
metadata:
  name: ultrafeeder
  namespace: adsb
spec:
  selector:
    app: ultrafeeder
  ports:
    - name: http
      port: 80
      targetPort: 80
    - name: beast
      port: 30005
      targetPort: 30005
      protocol: TCP
