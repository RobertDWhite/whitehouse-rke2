apiVersion: v1
kind: Service
metadata:
  name: openwebui
  namespace: ai-stack
spec:
  selector:
    app: openwebui
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
