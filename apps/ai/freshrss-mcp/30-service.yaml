apiVersion: v1
kind: Service
metadata:
  name: freshrss-mcp
  namespace: freshrss-mcp
spec:
  selector:
    app: freshrss-mcp
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
