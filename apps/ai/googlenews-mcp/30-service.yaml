apiVersion: v1
kind: Service
metadata:
  name: googlenews-mcp
  namespace: googlenews-mcp
spec:
  selector:
    app: googlenews-mcp
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
