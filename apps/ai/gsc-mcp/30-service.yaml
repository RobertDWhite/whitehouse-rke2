apiVersion: v1
kind: Service
metadata:
  name: gsc-mcp
  namespace: gsc-mcp
spec:
  selector:
    app: gsc-mcp
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
