apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - 00-namespace.yaml
  - 20-deployment.yaml
  - 30-service.yaml
  - 53-httproute-internal.yaml
  - 60-networkpolicy.yaml

generators:
  - ksops.yaml

images:
  # Built by github.com/RobertDWhite/gsc-mcp CI -> ghcr.io. Pin the
  # digest after the first build
  # (crane digest ghcr.io/robertdwhite/gsc-mcp:latest).
  - name: registry.internal.white.fm/gsc-mcp
    newName: ghcr.io/robertdwhite/gsc-mcp
    newTag: v0.1.0
