apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: invokeai-root
  namespace: invokeai
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: longhorn
