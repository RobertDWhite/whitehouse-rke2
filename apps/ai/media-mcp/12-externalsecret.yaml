apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: media-mcp-lazylibrarian
  namespace: media-mcp
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-shared
  target:
    name: media-mcp-lazylibrarian
    creationPolicy: Owner
  data:
    - secretKey: LAZYLIBRARIAN_KEY
      remoteRef:
        key: lhzqx4my7hvhuviwjekkgxafju/password
