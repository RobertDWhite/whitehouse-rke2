apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: media-mcp-internal
  namespace: media-mcp
spec:
  parentRefs:
    - name: main
      namespace: envoy-gateway-system
      sectionName: https-internal-white-fm
  hostnames:
    - media-mcp.internal.white.fm
  rules:
    - backendRefs:
        - name: media-mcp
          port: 8080
