apiVersion: v1
kind: Service
metadata:
  name: meerkat-mcp
  namespace: meerkat-mcp
spec:
  selector:
    app: meerkat-mcp
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
