apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: openclaw-api-keys
  namespace: openclaw
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-shared
  target:
    name: openclaw-api-keys
    creationPolicy: Owner
    template:
      data:
        # Only ANTHROPIC_TOKEN — populating ANTHROPIC_API_KEY with an OAuth
        # token causes the Anthropic SDK to send x-api-key (rejected) before
        # the OAuth-bearer code path runs.
        ANTHROPIC_TOKEN: "{{ .ANTHROPIC_TOKEN }}"
        NPM_CONFIG_CACHE: "/home/openclaw/.openclaw/.npm"
        OPENAI_API_KEY: "{{ .OPENAI_API_KEY }}"
        TELEGRAM_BOT_TOKEN: "{{ .TELEGRAM_BOT_TOKEN }}"
  data:
    - secretKey: ANTHROPIC_TOKEN
      remoteRef:
        key: "openclaw-api-keys/ANTHROPIC_TOKEN"
    - secretKey: OPENAI_API_KEY
      remoteRef:
        key: "openclaw-api-keys/OPENAI_API_KEY"
    - secretKey: TELEGRAM_BOT_TOKEN
      remoteRef:
        key: "openclaw-api-keys/TELEGRAM_BOT_TOKEN"
