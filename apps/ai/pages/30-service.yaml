apiVersion: v1
kind: Service
metadata:
  name: pages
  namespace: pages
spec:
  selector:
    app: pages
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
