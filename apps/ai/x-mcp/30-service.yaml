apiVersion: v1
kind: Service
metadata:
  name: x-mcp
  namespace: x-mcp
spec:
  selector:
    app: x-mcp
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
