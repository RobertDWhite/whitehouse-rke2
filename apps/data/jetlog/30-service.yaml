apiVersion: v1
kind: Service
metadata:
  name: jetlog
  namespace: jetlog
spec:
  selector:
    app: jetlog
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP
