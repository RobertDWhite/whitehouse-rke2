apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: weather-timemachine
  namespace: weather
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
