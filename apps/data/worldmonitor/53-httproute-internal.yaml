apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: worldmonitor-internal
  namespace: worldmonitor
spec:
  parentRefs:
    - name: main
      namespace: envoy-gateway-system
      sectionName: https-internal-white-fm
  hostnames:
    - worldmonitor.internal.white.fm
  rules:
    - backendRefs:
        - name: worldmonitor
          port: 80
