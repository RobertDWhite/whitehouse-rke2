apiVersion: v1
kind: Service
metadata:
  name: backoffice-postgres
  namespace: backoffice
spec:
  selector:
    app: backoffice-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
