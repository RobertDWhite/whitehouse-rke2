apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nodebyte-postgres
  namespace: nodebyte
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 2Gi
