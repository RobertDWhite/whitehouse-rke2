apiVersion: v1
kind: Service
metadata:
  name: scrypted
  namespace: scrypted
spec:
  selector:
    app: scrypted
  ports:
    - name: https
      port: 10443
      targetPort: 10443
      protocol: TCP
