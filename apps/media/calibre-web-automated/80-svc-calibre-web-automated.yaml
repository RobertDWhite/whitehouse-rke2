apiVersion: v1
kind: Service
metadata:
  name: calibre-web-automated
  namespace: calibre
spec:
  selector:
    app: calibre-web-automated
  ports:
    - port: 80
      targetPort: 8083
      protocol: TCP
