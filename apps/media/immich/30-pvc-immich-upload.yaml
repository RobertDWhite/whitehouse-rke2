apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-upload
  namespace: immich
spec:
  accessModes: [ReadWriteMany]
  resources:
    requests:
      storage: 10Ti
  storageClassName: ""
  volumeName: pv-immich-upload
