apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-photos
  namespace: immich
spec:
  accessModes: [ReadWriteMany]
  resources:
    requests:
      storage: 10Ti
  storageClassName: ""
  volumeName: pv-immich-photos
