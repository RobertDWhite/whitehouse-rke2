apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kavita-library
  namespace: kavita
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Ti
  volumeName: pv-kavita-library
  storageClassName: ""
