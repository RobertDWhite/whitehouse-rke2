apiVersion: batch/v1
kind: Job
metadata:
  name: fleet-migration
  namespace: fleet
spec:
  template:
    spec:
      imagePullSecrets:
        - name: ghcr-pull
      containers:
        - name: fleet-migration
          imagePullPolicy: Always
