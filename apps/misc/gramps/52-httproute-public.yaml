apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: gramps-public
  namespace: gramps
spec:
  parentRefs:
    - name: main
      namespace: envoy-gateway-system
      sectionName: https-white-fm
  hostnames:
    - ancestry.white.fm
  rules:
    - backendRefs:
        - name: gramps
          port: 80
