apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: gramps-internal
  namespace: gramps
spec:
  parentRefs:
    - name: main
      namespace: envoy-gateway-system
      sectionName: https-internal-white-fm
  hostnames:
    - ancestry.internal.white.fm
  rules:
    - backendRefs:
        - name: gramps
          port: 80
