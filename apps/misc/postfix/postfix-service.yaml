apiVersion: v1
kind: Service
metadata:
  name: postfix
  namespace: postfix

spec:
  selector:
    app: postfix
  ports:
    - name: smtp
      port: 25
      targetPort: 25
    - name: smtps
      port: 465
      targetPort: 465
