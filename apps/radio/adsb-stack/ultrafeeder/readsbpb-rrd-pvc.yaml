apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: readsbpb-rrd-v2
  namespace: adsb
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
  storageClassName: longhorn

