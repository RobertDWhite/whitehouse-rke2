apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ultrafeeder-globe-history
  namespace: adsb
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: longhorn
