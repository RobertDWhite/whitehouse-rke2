apiVersion: v1
kind: Service
metadata:
  name: openhamclock
  namespace: openhamclock
spec:
  selector:
    app: openhamclock
  ports:
    - name: http
      port: 3000
      targetPort: 3000
  type: ClusterIP
