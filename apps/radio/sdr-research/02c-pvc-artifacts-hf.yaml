apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sdr-artifacts-hf
  namespace: sdr-research
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: longhorn
  resources:
    requests:
      storage: 100Gi
