apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: maubot-data
  namespace: matrix
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: longhorn
