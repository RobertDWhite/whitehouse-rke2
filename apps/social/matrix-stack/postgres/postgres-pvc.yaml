apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: matrix-postgresql-pvc
  namespace: matrix
  annotations:
    longhorn.io/node-selector: "longhorn-ok"
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 50Gi
  storageClassName: longhorn