apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: openhamclock
  namespace: argocd
spec:
  project: apps
  source:
    repoURL: git@github.com:RobertDWhite/whitehouse-rke2.git
    targetRevision: main
    path: apps/radio/openhamclock
  destination:
    server: https://kubernetes.default.svc
    namespace: openhamclock
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
