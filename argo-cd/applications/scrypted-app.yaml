apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: scrypted
  namespace: argocd
spec:
  project: apps
  source:
    repoURL: git@github.com:RobertDWhite/whitehouse-rke2.git
    targetRevision: main
    path: apps/home/scrypted
    kustomize: {}
  destination:
    server: https://kubernetes.default.svc
    namespace: scrypted
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
