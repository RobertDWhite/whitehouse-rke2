apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: auth-white-fm-tls
  namespace: authentik
spec:
  secretName: auth-white-fm-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-dns
  dnsNames:
    - auth.white.fm
