apiVersion: v1
kind: Service
metadata:
  name: authentik-server-9443
  namespace: authentik
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/component: server
    app.kubernetes.io/instance: authentik
    app.kubernetes.io/name: authentik
  ports:
    - name: https
      port: 9443
      targetPort: 9443
