apiVersion: v1
kind: Service
metadata:
  name: authentik-proxy
  namespace: adsb
spec:
  type: ClusterIP
  ports:
    - port: 9000
      targetPort: 9000
      protocol: TCP
  externalName: authentik-server.authentik.svc.cluster.local
