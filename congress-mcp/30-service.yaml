apiVersion: v1
kind: Service
metadata:
  name: congress-mcp
  namespace: congress-mcp
spec:
  selector:
    app: congress-mcp
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
