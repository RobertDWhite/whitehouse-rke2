apiVersion: v1
kind: Service
metadata:
  name: dispatcharr-web
  namespace: dispatcharr
spec:
  selector:
    app: dispatcharr-web
  ports:
    - port: 9191
      targetPort: 9191
      protocol: TCP
