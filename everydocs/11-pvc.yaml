apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-everydocs-files
  namespace: everydocs
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Gi
  storageClassName: ""
  volumeName: pv-everydocs-files