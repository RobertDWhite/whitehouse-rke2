apiVersion: v1
kind: Service
metadata:
  name: everydocs-web
  namespace: everydocs
spec:
  type: LoadBalancer
  selector:
    app: everydocs-web
  ports:
  - port: 80
    targetPort: 80
  - port: 443
    targetPort: 443