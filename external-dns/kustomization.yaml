apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - serviceaccount.yaml
  - clusterrole.yaml
  - clusterrolebinding.yaml
  - deployment.yaml
  - deployment-pihole.yaml
  - deployment-pihole-b.yaml
  - deployment-pihole-c.yaml

generators:
  - ksops.yaml

images:
  - name: registry.k8s.io/external-dns/external-dns
    newTag: v0.20.0
