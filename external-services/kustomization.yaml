apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: external-services

resources:
  - 00-namespace.yaml
  - 12-service-authentik.yaml
  - 50-ingress-homeassistant.yaml
  - 51-ingress-homeassistant-internal.yaml

# Services, Ingresses, and Endpoints (all SOPS encrypted)
generators:
  - ksops.yaml
