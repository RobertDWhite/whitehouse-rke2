apiVersion: apps/v1
kind: Deployment
metadata:
  name: freshrss
  namespace: freshrss
spec:
  replicas: 1
  selector:
    matchLabels:
      app: freshrss
  template:
    metadata:
      labels:
        app: freshrss
    spec:
      initContainers:
        - name: install-aiassistant
          image: alpine/git
          command:
            - sh
            - -c
            - |
              echo "Installing FreshRSS AI Assistant..." && \
              mkdir -p /extensions/xExtension-AIAssistant && \
              rm -rf /extensions/xExtension-AIAssistant/* && \
              git clone https://github.com/cvlc/freshrss-ai-assistant /extensions/xExtension-AIAssistant --depth 1 || true && \
              chown -R 1000:1000 /extensions || true
          volumeMounts:
            - name: extensions
              mountPath: /extensions

        - name: install-feeddigest
          image: alpine/git
          command:
            - sh
            - -c
            - |
              echo "Installing FeedDigest..." && \
              mkdir -p /extensions/xExtension-FeedDigest && \
              rm -rf /extensions/xExtension-FeedDigest/* && \
              git clone https://github.com/fengchang/xExtension-FeedDigest /extensions/xExtension-FeedDigest --depth 1 || true && \
              chown -R 1000:1000 /extensions || true
          volumeMounts:
            - name: extensions
              mountPath: /extensions

        - name: install-wordhighlighter
          image: alpine/git
          command:
            - sh
            - -c
            - |
              echo "Installing WordHighlighter..." && \
              mkdir -p /extensions/xExtension-WordHighlighter && \
              rm -rf /extensions/xExtension-WordHighlighter/* && \
              git clone https://github.com/FreshRSS/Extensions.git /tmp/freshrss-extensions --depth 1 || true && \
              cp -r /tmp/freshrss-extensions/xExtension-WordHighlighter/* /extensions/xExtension-WordHighlighter/ && \
              chown -R 1000:1000 /extensions || true
          volumeMounts:
            - name: extensions
              mountPath: /extensions

      containers:
        - name: freshrss
          image: lscr.io/linuxserver/freshrss:1.27.1-ls285 # renovate: datasource=docker depName=lscr.io/linuxserver/freshrss
          ports:
            - containerPort: 80
          env:
            - name: PUID
              value: "1000"
            - name: PGID
              value: "1000"
            - name: TZ
              value: "America/New_York"
          volumeMounts:
            - name: data
              mountPath: /config
            - name: extensions
              mountPath: /config/www/freshrss/extensions

      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: freshrss-data
        - name: extensions
          persistentVolumeClaim:
            claimName: freshrss-extensions
