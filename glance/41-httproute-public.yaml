apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: glance-public
  namespace: glance
spec:
  parentRefs:
    - name: main
      namespace: envoy-gateway-system
      sectionName: https-white-fm
  hostnames:
    - home.white.fm
  rules:
    - backendRefs:
        - name: glance
          port: 8080
