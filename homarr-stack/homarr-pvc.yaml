apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: homarr-database-longhorn
  namespace: homarr
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: longhorn

