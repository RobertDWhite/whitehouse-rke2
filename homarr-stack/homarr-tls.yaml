apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: homarr2-white-fm-tls
  namespace: homarr
spec:
  secretName: homarr2-white-fm-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-dns
  dnsNames:
    - homarr.white.fm
