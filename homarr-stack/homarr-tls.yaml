apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: homarr-white-fm-tls
  namespace: homarr
spec:
  secretName: homarr-white-fm-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-dns
  dnsNames:
    - home.white.fm
