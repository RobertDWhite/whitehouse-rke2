apiVersion: v1
kind: ConfigMap
metadata:
  name: homepage-settings
  namespace: homepage
data:
  settings.yaml: |
    layout:
      widgets:
        - [kubernetes, resources, unifi_console]
        - [datetime, openweathermap, search]  