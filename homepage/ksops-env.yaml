apiVersion: viaduct.ai/v1
kind: Ksops
metadata:
  name: homepage-config-generator
  annotations:
    config.kubernetes.io/function: |
      exec:
        path: ksops
    config.kubernetes.io/local-config: "true"
files:
  - homepage_env.yaml
  - hostaliases-patch.sops.yaml

