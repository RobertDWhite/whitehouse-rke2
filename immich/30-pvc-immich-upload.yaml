apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-pvc
  namespace: immich
spec:
  accessModes: ["ReadWriteMany"]
  storageClassName: pv-immich-upload
  resources:
    requests:
      storage: 10Ti