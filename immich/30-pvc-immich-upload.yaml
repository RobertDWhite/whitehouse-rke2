apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-pvc
  namespace: immich
spec:
  accessModes: ["ReadWriteMany"]
  storageClassName: synology-nfs-immich
  resources:
    requests:
      storage: 10Ti