apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: photo-pvc
  namespace: immich
spec:
  accessModes: ["ReadWriteMany"]
  storageClassName: synology-nfs-photo
  resources:
    requests:
      storage: 10Ti
