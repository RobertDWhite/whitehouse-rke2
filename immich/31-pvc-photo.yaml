apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: photo-pvc
  namespace: immich
spec:
  accessModes: ["ReadWriteMany"]
  storageClassName: pv-immich-photos
  resources:
    requests:
      storage: 10Ti
