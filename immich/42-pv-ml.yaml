apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-immich-ml-cache
spec:
  capacity:
    storage: 5Ti
  accessModes: [ReadWriteMany]
  persistentVolumeReclaimPolicy: Retain
  mountOptions:
    - nfsvers=4.1
  nfs:
    server: 10.100.1.20
    path: /volume2/immich/immich-ml-cache