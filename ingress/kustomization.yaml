apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Network policies are now centralized in network-policies/
  - rke2-ingress-nginx-helmchartconfig.yaml
  - service-rke2-ingress-nginx-controller.yaml
