apiVersion: v1
kind: Service
metadata:
  name: jetlog-mcp
  namespace: jetlog-mcp
spec:
  selector:
    app: jetlog-mcp
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
