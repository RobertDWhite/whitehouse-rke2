apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jetlog-data
  namespace: jetlog
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: longhorn
