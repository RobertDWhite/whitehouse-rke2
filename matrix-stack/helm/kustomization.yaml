apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

generators:
  - ksops-values.yaml

helmCharts:
  - name: matrix-stack
    repo: https://github.com/element-hq/ess-helm.git
    version: main
    releaseName: matrix-ess
    namespace: matrix
    valuesFile: values.sops.yaml