apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

generators:
  - ksops-values.yaml

helmCharts:
  - name: matrix-stack
    releaseName: matrix-ess
    namespace: matrix
    repo: oci://ghcr.io/element-hq/ess-helm
    version: 25.9.0
    valuesFile: values.sops.yaml
