apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: matrix-redis-pvc
  namespace: matrix
  annotations:
    longhorn.io/node-selector: "longhorn-ok"
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 10Gi
  storageClassName: longhorn