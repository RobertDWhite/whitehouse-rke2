apiVersion: apps/v1
kind: Deployment
metadata:
  name: matrix-webhooks
  namespace: matrix
spec:
  replicas: 1
  selector:
    matchLabels:
      app: matrix-webhooks
  template:
    metadata:
      labels:
        app: matrix-webhooks
    spec:
      nodeSelector:
        kubernetes.io/arch: amd64
      containers:
        - name: webhooks
          image: jceloria/matrix-encrypted-webhooks:latest@sha256:f5ba12e4a5ba179217f340750aeaad881b1a6c5d9bcc7d6b02b07fcfd1575e2f
          ports:
            - containerPort: 8000
          envFrom:
            - secretRef:
                name: matrix-webhooks-secret
          volumeMounts:
            - name: config
              mountPath: /webhooks-config
      volumes:
        - name: config
          persistentVolumeClaim:
            claimName: matrix-webhooks-config
