apiVersion: v1
kind: Service
metadata:
  name: nodebyte-mcp
  namespace: nodebyte-mcp
spec:
  selector:
    app: nodebyte-mcp
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
