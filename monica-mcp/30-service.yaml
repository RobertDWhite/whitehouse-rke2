apiVersion: v1
kind: Service
metadata:
  name: monica-mcp
  namespace: monica-mcp
spec:
  selector:
    app: monica-mcp
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
