apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: monica-data
  namespace: monica
  annotations:
    helm.sh/resource-policy: keep
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi