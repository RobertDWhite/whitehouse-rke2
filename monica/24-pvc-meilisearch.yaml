apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: monica-meilisearch-data
  namespace: monica
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 5Gi
