apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Network policies are namespace-scoped; each resource specifies its own namespace.
# Do NOT set a global namespace here.

resources:
  - sdr-research.yaml
  - observability.yaml
  - matrix.yaml
  - authentik.yaml
  - misp.yaml
  - mastodon.yaml
  - ai-stack.yaml
  - immich.yaml
  - uptime-kuma.yaml
  - freshrss.yaml
  - paperless.yaml
  - dispatcharr.yaml
  - infrastructure.yaml
  - simple-web-apps.yaml
