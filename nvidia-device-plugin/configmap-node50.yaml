apiVersion: v1
kind: ConfigMap
metadata:
  name: nvidia-device-plugin-config-node50
  namespace: kube-system
data:
  config.yaml: |
    version: v1
    flags:
      migStrategy: none
    sharing:
      timeSlicing:
        resources:
          - name: nvidia.com/gpu
            replicas: 2
