apiVersion: v1
kind: Service
metadata:
  name: syslog
  namespace: observability
spec:
  type: LoadBalancer
  selector:
    app: promtail
  ports:
    - name: syslog-tcp
      port: 1514
      protocol: TCP
    - name: syslog-udp
      port: 1514
      protocol: UDP