apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: appstore-connect-api
  namespace: observability
spec:
  refreshInterval: 24h
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-shared
  target:
    name: appstore-connect-api
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: kza42imakfeico5ocrlffxazya
