apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: myspeed-data
  namespace: myspeed
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: longhorn