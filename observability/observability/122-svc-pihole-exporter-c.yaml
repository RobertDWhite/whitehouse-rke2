apiVersion: v1
kind: Service
metadata:
  name: pihole-exporter-c
  namespace: observability
  labels:
    app: pihole-exporter
spec:
  selector:
    app: pihole-exporter-c
  ports:
    - name: metrics
      port: 9617
      targetPort: 9617
