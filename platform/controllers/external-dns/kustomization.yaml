apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - dnsendpoints-crd.yaml
  - serviceaccount.yaml
  - clusterrole.yaml
  - clusterrolebinding.yaml
  - deployment.yaml
  - serviceaccount-records.yaml
  - clusterrole-records.yaml
  - clusterrolebinding-records.yaml
  - deployment-records.yaml
  - service-metrics.yaml

generators:
  - ksops.yaml

images:
  - name: registry.k8s.io/external-dns/external-dns
    newTag: v0.22.0
