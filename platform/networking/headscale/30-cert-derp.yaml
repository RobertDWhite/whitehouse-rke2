apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: derp-white-fm-tls
  namespace: headscale
spec:
  secretName: derp-white-fm-tls
  dnsNames:
    - derp.white.fm
  issuerRef:
    name: letsencrypt-dns
    kind: ClusterIssuer
