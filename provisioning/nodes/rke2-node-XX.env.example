# Copy to rke2-node-<NN>.env and fill in. One file per node.
NODE_HOSTNAME=rke2-node-XX
NODE_IP=10.99.5.XX
# "server" (control plane) or "agent" (worker)
RKE2_TYPE=agent
# Optional per-node VM sizing (falls back to _defaults.env):
# VM_CPU=8
# VM_MEM_MB=16384
# DISK_GB=200
