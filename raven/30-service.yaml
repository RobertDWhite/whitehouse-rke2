apiVersion: v1
kind: Service
metadata:
  name: raven
spec:
  selector:
    app: raven
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
    - name: ws
      protocol: TCP
      port: 5678
      targetPort: ws
  type: ClusterIP
