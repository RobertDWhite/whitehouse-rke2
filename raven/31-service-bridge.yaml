apiVersion: v1
kind: Service
metadata:
  name: raven-bridge
spec:
  type: ExternalName
  externalName: misp-raven-bridge.misp.svc.cluster.local
  ports:
    - name: ws
      port: 5678
      protocol: TCP
