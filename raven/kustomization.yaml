apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: raven

resources:
  - 00-namespace.yaml
  - 10-configmap-nginx.yaml
  - 11-configmap-index.yaml
  - 20-deployment.yaml
  - 30-service.yaml
  - 40-ingress.yaml

images:
  - name: nginx
    newTag: "1.27-alpine"
