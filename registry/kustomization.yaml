apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - 00-namespace.yaml
  - 05-config.yaml
#  - 10-pvc.yaml
  - 20-deployment.yaml
  - 30-svc.yaml
  # 45-metallb-registry.yaml removed: the :5000 HTTP LoadBalancer was the
  # remaining unauthenticated, MITM-able registry surface. All consumers now
  # use registry.internal.white.fm (HTTPS via Envoy → registry ClusterIP:5000).
  
generators:
  - ksops.yaml

images:
  - name: registry
    newTag: "3"
    digest: "sha256:85347ed2ecde64161c7a4788a4d7d3dcc9d6f86f7be95834022e3c6a423a945a"
