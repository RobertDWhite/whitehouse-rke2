apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sdr-artifacts
  namespace: sdr-research
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-single
  resources:
    requests:
      storage: 120Gi
