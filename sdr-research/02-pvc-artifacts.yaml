apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sdr-artifacts
  namespace: sdr-research
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 500Gi
