apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sdr-artifacts-rwx
  namespace: sdr-research
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: longhorn
  resources:
    requests:
      storage: 500Gi
