apiVersion: v1
kind: Service
metadata:
  name: rtl-tcp-70cm
  namespace: sdr-research
spec:
  selector:
    app: rtl-sdr-tcp-70cm
  ports:
    - name: rtl-tcp-70cm
      port: 1235
      targetPort: 1235
