apiVersion: apps/v1
kind: Deployment
metadata:
  name: acars-decoder
  namespace: sdr-research
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: acars-decoder
  template:
    metadata:
      labels:
        app: acars-decoder
    spec:
      nodeSelector:
        kubernetes.io/hostname: rke2-node-14
      restartPolicy: Always
      containers:
        - name: acars-decoder
          image: debian:stable-slim
          env:
            - name: MIN_FILE_AGE_SEC
              value: "15"
          command:
            - /bin/sh
            - -c
            - |
              set -e
              echo "Installing ACARS decoder dependencies"
              apt-get update -qq
              apt-get install -y --no-install-recommends multimon-ng sox
              rm -rf /var/lib/apt/lists/*

              cp /scripts/acars_decode.sh /tmp/acars_decode.sh
              chmod +x /tmp/acars_decode.sh

              echo "Starting ACARS decoder loop"
              while true; do
                /tmp/acars_decode.sh
                sleep 10
              done
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 256Mi
          volumeMounts:
            - name: artifacts
              mountPath: /data
            - name: scripts
              mountPath: /scripts
      volumes:
        - name: artifacts
          persistentVolumeClaim:
            claimName: sdr-artifacts-rwx
        - name: scripts
          configMap:
            name: acars-decoder-scripts
            defaultMode: 0755
