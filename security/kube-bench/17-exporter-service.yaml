apiVersion: v1
kind: Service
metadata:
  name: kube-bench-exporter
  namespace: kube-bench
  labels:
    app: kube-bench-exporter
spec:
  selector:
    app: kube-bench-exporter
  ports:
    - name: metrics
      port: 9100
      targetPort: metrics
      protocol: TCP
