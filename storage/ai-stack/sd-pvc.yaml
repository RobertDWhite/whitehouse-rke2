apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: invokeai-data
  namespace: ai-stack
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 100Gi
