apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sd-webui-data
  namespace: ai-stack
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn   # CHANGE if needed
  resources:
    requests:
      storage: 100Gi