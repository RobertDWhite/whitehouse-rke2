apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: homarr-database-longhorn
  namespace: homarr
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn

  resources:
    requests:
      storage: 1Gi
