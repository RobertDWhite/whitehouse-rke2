apiVersion: v1
kind: Service
metadata:
  name: obfs4-bridge-metrics
  namespace: tor
  labels:
    app: obfs4-bridge
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app: obfs4-bridge
  ports:
    - name: metrics
      port: 9035
      targetPort: metrics
      protocol: TCP
